<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".ForgotPassword">

  <TextView
      android:id="@+id/ForgotPassword2"
      android:layout_width="361dp"
      android:layout_height="117dp"
      android:layout_marginStart="50sp"
      android:layout_marginTop="50sp"
      android:layout_marginEnd="50sp"
      android:layout_marginBottom="50sp"
      android:gravity="center"
      android:text="Forgot Password :O?"
      android:textColor="#FF6347"
      android:textSize="35sp"
      android:textStyle="bold" />

  <EditText
      android:id="@+id/EmailForgotPassword"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@id/ForgotPassword2"
      android:layout_marginStart="10dp"
      android:layout_marginTop="10dp"
      android:layout_marginEnd="10dp"
      android:layout_marginBottom="10dp"
      android:background="#30ffffff"
      android:drawableLeft="@drawable/ic_baseline_person_24"
      android:hint="Enter your email here"
      android:importantForAutofill="no"
      android:padding="20dp"
      android:textColor="@color/white"
      android:textColorHint="@color/white" />

  <TextView
      android:id="@+id/SomeReassuringText"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@id/EmailForgotPassword"
      android:layout_marginStart="50sp"
      android:layout_marginTop="10sp"
      android:layout_marginEnd="50sp"
      android:layout_marginBottom="10sp"
      android:gravity="center"
      android:text="Don't worry, enter your email and we will get you back in!"
      android:textColor="@color/white"
      android:textSize="15sp"
      android:textStyle="bold" />

  <Button
      android:id="@+id/ResetPasswordButton"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@id/SomeReassuringText"
      android:layout_marginStart="50sp"
      android:layout_marginTop="10sp"
      android:layout_marginEnd="50sp"
      android:layout_marginBottom="50sp"
      android:gravity="center"
      android:backgroundTint="@color/design_default_color_secondary"
      android:text="Reset password"
      />

  <TextView
      android:id="@+id/backbtnForgotPassword"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_below="@id/ResetPasswordButton"
      android:layout_centerHorizontal="true"
      android:layout_marginStart="5dp"
      android:layout_marginTop="5dp"
      android:layout_marginEnd="5dp"
      android:layout_marginBottom="5dp"
      android:text="GO BACK!!!!!"
      android:textColor="#FF6347" />


</RelativeLayout
>